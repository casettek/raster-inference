[package]
name = "dsl-macros"
version = "0.1.0"
edition = "2021"
description = "Proc-macro companion crate for raster-inference's DSL module"

[lib]
proc-macro = true

[dependencies]
quote = "1"
syn = { version = "2", features = ["full"] }
